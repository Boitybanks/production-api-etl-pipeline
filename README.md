# production-api-etl-pipeline
Production-grade ETL pipeline that extracts cryptocurrency market data from the CoinGecko API, transforms it using Python, and loads it into PostgreSQL. Includes Docker, Airflow, logging, testing, and scheduling.
